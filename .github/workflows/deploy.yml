# NOTA: Este workflow quedó desactualizado — apuntaba al VPS antiguo (31.97.8.51),
# que fue eliminado permanentemente. El deploy actual a AWS (bastión + EC2 + RDS)
# se realiza manualmente. No reactivar este workflow sin actualizar host/credenciales
# al entorno AWS vigente.
name: Deploy to VPS (DESACTIVADO)

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy deshabilitado
        run: echo "Este workflow está deshabilitado. El deploy se hace manualmente a AWS."
